# EduNova AI - Backend Requirements (LEAN, Render-friendly)
# Tested with Python 3.10, 3.11, 3.12, 3.13.
#
# This file deliberately omits the heavy AI deps (langgraph, langchain, deepeval).
# The application has built-in fallbacks for both — see app/agents/workflow.py
# (sequential runner) and app/evaluation/deepeval_evaluator.py (heuristic).
#
# If you want the FULL stack with real LangGraph + DeepEval, additionally:
#     pip install -r requirements-full.txt

# --- Core web framework ---
fastapi>=0.115.0,<0.116
uvicorn[standard]>=0.32.0,<0.33
python-multipart>=0.0.12

# --- Data & validation ---
pydantic>=2.9.2,<3.0
pydantic-settings>=2.5.2,<3.0
email-validator>=2.2.0

# --- Database (async only — no psycopg2 needed) ---
sqlalchemy>=2.0.35,<3.0
aiosqlite>=0.20.0           # SQLite async driver (dev fallback)
asyncpg>=0.29.0             # PostgreSQL async driver (prod / Render)

# --- LLM providers (light SDKs, both optional at runtime) ---
openai>=1.51.2
google-generativeai>=0.8.3

# --- Auth-ready (JWT) ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- Networking / utilities ---
httpx>=0.27.2
python-dotenv>=1.0.1
